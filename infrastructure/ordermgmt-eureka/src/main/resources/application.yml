spring:
  application:
    name: ordermgmt-eureka
  config:
    import: optional:configserver:${CONFIG_SERVER_URL}
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 5000
        max-attempts: 10
        max-interval: 10000
        multiplier: 1.5